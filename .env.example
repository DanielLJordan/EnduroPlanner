# Copy this file to .env and fill in your values.
# .env is gitignored — never commit real secrets.

# Team password for the login gate (default: "arete" if not set)
# Set this as an environment variable in Cloudflare Pages / your hosting platform.
VITE_TEAM_PASSWORD=your-team-password-here
